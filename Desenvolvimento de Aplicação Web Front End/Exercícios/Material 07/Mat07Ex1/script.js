// 1 - Faça um algoritmo para calcular a área de uma circunferência, considerando a fórmula: 
// ÁREA = π * RAIO^2
let r = Number(window.prompt("Informe o valor do raio: "));
let area = Math.pow(r,2) * Math.PI;
alert("Área = "+ area);
