// 2 - Faça um algoritmo que calcule a área de um triângulo, considerando a fórmula:
// Area = (base x altura)/2
// Considere as dimensões base e altura em metros

let base = Number(window.prompt("Informe o valor do base: "));
let altura = Number(window.prompt("Informe o valor do altura: "));
let area = base * altura;
alert("Área = "+ area);